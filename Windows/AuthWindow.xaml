<Window x:Class="horsesProj.Windows.AuthWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:horsesProj.Windows"
        mc:Ignorable="d"
        Title="Вход" 
        Height="450" 
        Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="6*" />
        </Grid.ColumnDefinitions>

        <Button VerticalAlignment="Top" Content="Назад" Click="Button_Click" />

        <StackPanel Grid.Column="1" HorizontalAlignment="Center" VerticalAlignment="Center">
            <UniformGrid Rows="2" Columns="2" Margin="10">
                <TextBlock Text="Логин:" />
                <TextBox x:Name="login" />
                <TextBlock Text="Пароль:" />
                <PasswordBox x:Name="password" />
            </UniformGrid>
            <StackPanel Orientation="Horizontal" Margin="10">
                <Button Content="Вход" Click="Button_Click_1" />
                <Button Content="Я болельщик" Click="Button_Click_2" />
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
